<h1>Wikinimous</h1>

<%= link_to "New article", new_article_path %>

<ul>
  <% @articles.each do |article| %>
    <li><%= link_to article.title, article_path(article) %></li>
    <li><%= link_to article.content, article_path(article) %></li>
  <% end %>
</ul>
